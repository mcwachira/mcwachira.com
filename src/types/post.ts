export type PostTypes ={
  post:{
      url: string;
    image: string;
    title: string;
    description: string;
    category: string;
    date: string;
    timeToRead: number; // Assuming timeToRead is in minutes
}
};